#ifndef _CLUSTERING_INIT_
#define _CLUSTERING_INIT_

// Declare functions of clustering_init.c file
node **Init_1(hashtable *, int, int, int);

node **Init_2(hashtable *, int, int);

int same_centroids(node **, int);

#endif